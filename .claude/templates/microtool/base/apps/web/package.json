{
  "name": "@{{WORKER_NAME}}/web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "wrangler pages deploy dist --project-name={{WORKER_NAME}}-web",
    "deploy:dev": "wrangler pages deploy dist --project-name={{WORKER_NAME}}-web --branch=dev",
    "deploy:staging": "wrangler pages deploy dist --project-name={{WORKER_NAME}}-web --branch=staging",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@{{WORKER_NAME}}/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "wrangler": "^3.0.0"
  }
}
