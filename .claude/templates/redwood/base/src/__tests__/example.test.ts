import { describe, it, expect } from 'vitest';

describe('Example', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });

  it('adds numbers correctly', () => {
    expect(1 + 1).toBe(2);
  });
});
